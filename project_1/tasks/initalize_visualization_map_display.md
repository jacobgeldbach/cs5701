# Task 2: With the map file read in and processed into the 40x40 2D list, initailize the pygame graphics library and the display the map based on the cell structs that define each cell.

# Context: The pygame visualization library will need to generate and display a 40x40 grid with color coating per cell detailing each cell. Each cell will be a color based on its terrain type that makes sense i.e. water is blue, mountains brown, field being the default I would prefer to be a lighter gray instead of green. Since the search algorithms are not implemented yet, this will just be a static display of the 40x40 grid to begin, but will need to be intialized so that its able to be updated in real time once the algorithm is running. The plan is for the algorithms to show if a cell is currently placed on the OpenList will be to turn that cell a bit darker shade of the color it is to show how that algorithm is exploring the cells based on how it is implemented. The visualization will be a seperate window that appears once the process has read and processed the target map. No cost will be show in the visulization.
