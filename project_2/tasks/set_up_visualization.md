# Task 1: Set up main.py and visualization.py

# Description: Call the needed pygame library initailization for the drawing of the 6x7 board that populates with the circular connect4 pieces when played on the board.

# Context: Set up the main function in main.py that reads in the programs arguments and using pygame draws the initial board. To start implement it so I can play against an AI that essentially just drops one in the closed column to the left every time its turn is up and I can drop one wherever. I will replace their dumb decision with the Minimax algorithm when this is all set up. This first task is all about setting the 6x7 int double list with the correct value at a coordinate based on who is playing and taking in the humans input.
